{
  "name": "trainer-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "biome check .",
    "typecheck": "turbo run typecheck",
    "format": "biome format . --write"
  },
  "devDependencies": {
    "turbo": "^2.0.14",
    "typescript": "^5.6.3",
    "@biomejs/biome": "^1.9.4"
  }
}
