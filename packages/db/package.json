{
  "name": "@trainer/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'Add db lint rules'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trainer/types": "workspace:*"
  }
}
