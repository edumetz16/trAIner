# @trainer/nest-functions

NestJS app preparada para desplegarse como Firebase Cloud Function HTTP (`api`).

## Uso

1. Instala dependencias del monorepo.
2. Compila con `pnpm --filter @trainer/nest-functions build`.
3. Ejecuta emulador con `pnpm --filter @trainer/nest-functions serve:functions`.
